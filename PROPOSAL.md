# POP3 UIDL 增量备份会话核心

本地申报候选材料，2026-09-22；模块 `cheng-jun56/pop3`，版本 `0.5.0`。团队的公开仓库可能还是先前提交，本次没有推送；最终表单必须指向团队实际上传版本。

## 要解决的任务

围绕 UIDL 清单和原始邮件拉取构造轻量备份程序，并明确 DELE/QUIT 的事务边界，避免把删除请求与成功提交混淆。

以下是目标任务和可复现工程证据，不虚构客户、存量部署或采用人数。

## 现有工作与新增贡献

SMTP/MIME 已有邻接组件；本项目的 POP3 取信与删除提交语义不同于 IMAP 同步和 Maildir 归档。没有宣称整个邮件生态空白，也没有把多个层写成同一功能。 检索原始响应在总交付包的创新性复核目录保存。

MoonBit 实现命令/会话、增量 CRLF、多行终止和点转义；Node 负责 socket、TLS/STLS、超时与取消。



## 可复现路径

仓库附编译引擎；修改源码后先构建。参考工具的额外依赖与环境变量见 TESTING.md；测试创建的网络服务仅在本机。

```sh
node tools/test-extensions.mjs
```

本轮 APOP/CAPA 认证扩展及分片回复、二进制拉取、QUIT、超时/取消路径验证通过；UIDL 命令由核心测试覆盖，完整备份应用尚未提供，不能据此宣称增量备份端到端验收。 本轮 JS/WasmGC 核心测试及 JS 构建通过，原始日志见 [本轮验证](evidence/innovation-review-20260922/results.json)。测试数量证明所列范围，不能代替创新性论证或推断正式审核通过。

## 边界与来源

这里只提供协议与客户端，完整备份调度/去重数据库由应用承担。QUIT 丢响应时提交状态不确定，不能承诺已撤销删除。

许可证与来源沿用仓库现有 LICENSE/第三方说明，不将标准、算法、词库或参考软件写成本项目发明。查重不是对全生态不存在的证明，日期、相邻项与未覆盖范围见 [DUPLICATION.md](DUPLICATION.md)。
